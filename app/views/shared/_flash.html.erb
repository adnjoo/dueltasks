<div class="fixed bottom-4 right-4 z-50">
  <% flash.each do |key, value| %>
    <div class="flash-message <%= flash_class_for(key) %> p-4 mb-4 rounded-lg border">
      <p class="font-semibold"><%= value %></p>
    </div>
  <% end %>
</div>
<script>
  $(".flash-message" ).fadeOut(10000);
</script>