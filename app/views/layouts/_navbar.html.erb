<nav class="bg-black text-white p-5 shadow-none border-b-4 border-white">
  <div class="container mx-auto flex justify-between items-center">
    <div class="text-2xl font-extrabold uppercase">
      XTrack
    </div>
    <ul class="flex space-x-6 text-xl">
      <li><a href="/" class="hover:text-gray-500">Home</a></li>
      <% if user_signed_in? %>
        <li><%= link_to "Log out", destroy_user_session_path, method: :delete, class: "hover:text-gray-500" %></li>
      <% else %>
        <li><%= link_to "Login", new_user_session_path, class: "hover:text-gray-500" %></li>
      <% end %>
    </ul>
  </div>
</nav>
